import type { Meta, StoryObj } from "storybook-solidjs-vite";

const meta: Meta = {
  title: "Welcome",
  render: () => <div>Component library demos go here</div>,
};

export default meta;

export const Default: StoryObj = {};
